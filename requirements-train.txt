# ACP Learn pipeline (Hydra + Lightning + WANDB)
torch>=2.0
lightning>=2.0
lightning-utilities>=0.10
torchmetrics>=1.0
hydra-core>=1.3
omegaconf>=2.3
numpy>=1.24
pandas>=2.0
scikit-learn>=1.2
PyYAML>=6.0
# Included because WANDB is the default configured logger.
wandb>=0.16
