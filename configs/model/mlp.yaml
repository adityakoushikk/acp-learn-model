# Default MLP (matches notebook: 3 hidden layers, dropout)
_target_: acp_learn.models.acp_module.ACPLitModule

net:
  _target_: acp_learn.models.architectures.mlp.DeepMLP
  input_dim: ???  # set by datamodule
  hidden_dims: [192, 96, 64]
  dropout_rates: [0.6, 0.5, 0.2]
  activation: relu

optimizer:
  _target_: torch.optim.Adam
  lr: 0.001
  weight_decay: 0.0

loss: binary_crossentropy
